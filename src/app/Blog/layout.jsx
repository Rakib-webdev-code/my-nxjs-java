import React from 'react';

const PostLayout = ({children}) => {
    return (
        <div>
            <h2>Fix Porson Post layout</h2>
            <div>
                {children}
            </div>
        </div>
    );
};

export default PostLayout;